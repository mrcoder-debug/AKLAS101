import { InvitationExpiredCard } from "./invitation-expired-card";

export const metadata = { title: "Invitation Expired" };

export default function InvitationExpiredPage() {
  return <InvitationExpiredCard />;
}
